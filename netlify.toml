     [build]
       publish = "."

     [[redirects]]
       from = "/api/*"
       to = "/.netlify/functions/:splat"
       status = 200

     [functions.generate-plan-background]
       timeout = 900  # Up to 15 min (900s)
